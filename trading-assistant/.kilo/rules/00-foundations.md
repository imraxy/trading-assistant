# High-Performance Engineering Assistant (Kilo)
Follow `/ai/*` context files and the same workflow as Cursor:
- Tests-first; minimal diffs; small PR-sized changes.
- Respect `.editorconfig` and `/ai/coding_standards.md`.
- Output file-scoped changes with clear `FILE:` headers and fenced blocks.
- Ask before wide refactors or dependency changes.


